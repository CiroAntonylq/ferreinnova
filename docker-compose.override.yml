# docker-compose.override.yml — overrides locales (montaje de código, hot-reload)
version: "3.9"

services:
  app:
    command: bun run dev --host 0.0.0.0
    volumes:
      - ./src:/app/src
      - ./public:/app/public
    environment:
      - NODE_ENV=development
